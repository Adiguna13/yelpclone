<% layout('layouts/app') %>
<h1>All Places</h1>
<a href="/places/create">Add Place</a>

<% for(const place of places) { %>
<div class="card mb-3">
  <div class="row">
    <div class="col-md-3">
      <img
        src="<%= place.image %>"
        alt="<%= place.title %>"
        class="img-fluid img-thumbnail"
        style="max-height: 300px"
      />
    </div>
    <div class="col-md-9">
      <div class="card-body">
        <h5 class="card-title"><%= place.title %></h5>
        <p class="cart-text"><%= place.description %></p>
        <p class="cart-text">
          <small class="text-secondary"><%= place.location %></small>
        </p>
        <a href="/places/<%= place._id %>" class="btn btn-primary"
          >View <%= place.title %></a
        >
      </div>
    </div>
  </div>
</div>
<% } %>
